# Pooler (puerto 6543): uso en runtime / Prisma Client
DATABASE_URL="postgresql://usuario:password@host:6543/postgres"

# Conexión directa (puerto 5432): solo para migraciones (evita error "prepared statement already exists")
DIRECT_URL="postgresql://usuario:password@host:5432/postgres"

PORT=3000
CORS_ORIGIN=http://localhost:5173